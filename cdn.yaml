kind: CDN
version: '1'
metadata:
  envTypes:
    - dev
data:
  redirects:
    rules:
      - name: redirect-sf
        when:
          reqProperty: path
          equals: dsfdfs
        action:
          type: redirect
          location: dsfsdfdsfsdfsdf
      - name: dsfsdf-relative
        when:
          reqProperty: path
          equals: dsdsf
        action:
          type: redirect
          location: /anotherpagekjhk
